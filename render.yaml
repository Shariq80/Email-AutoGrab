   services:
     - type: web
       name: job-application-system
       env: python
       buildCommand: pip install -r requirements.txt
       startCommand: gunicorn job_application_system.wsgi:application
       envVars:
         - key: DJANGO_SECRET_KEY
           generateValue: true
         - key: DJANGO_DEBUG
           value: false